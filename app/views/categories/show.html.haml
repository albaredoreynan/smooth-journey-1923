.block
  .content
    %h2= "Category: #{@category.category_name}"
    .inner
      %p
        %b Description:
        = @category.category_description
      %p
        %b Restaurant category:
        = @category.restaurant_category_id
      = link_to 'Edit', edit_category_path(@category)
      |
      \#{link_to 'Back', categories_path}
