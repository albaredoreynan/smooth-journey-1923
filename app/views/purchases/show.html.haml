.block
  .content
    /%h2.title New purchase
    .inner
      %p#notice= notice
      %p
        %b Purchase date:
        = @purchase.purchase_date.strftime("%B %d, %Y")
      %p
        %b Invoice:
        = @purchase.invoice_id
      %p
        %b Supplier:
        - supplierId = @purchase.supplier_id
        - supplier = Supplier.find(supplierId)
        = supplier.name
      %p
        %b Branch:
        - branchId = @purchase.branch_id
        - branch = Branch.find(branchId)
        = branch.location
      %ol
        - for purchase_item in @purchase.purchaserows
          %hr/
          %b Inventory item:
          - inventoryId = purchase_item.item_id
          - item = Item.find(inventoryId)
          = item.name
          %br/
          %b Purchase unit:
          - unitId = purchase_item.unit_id
          - unit = Unit.find(unitId)
          = unit.name
          %br/
          %b Purchase unit cost:
          = number_with_precision(purchase_item.unit_cost, :precision => 2)
          %br/
          %b Purchase quantity:
          = number_with_precision(purchase_item.quantity, :precision => 0)
          %br/
          %b Purchase amount:
          = number_with_precision(purchase_item.amount, :precision => 2)
          %br/
          %b Vat type:
          = purchase_item.vat_type
          %br/
          %b Vat amount:
          = number_with_precision(purchase_item.vat_amount, :precision => 2)
          %br/
          %b Net amount:
          = number_with_precision(purchase_item.net_amount, :precision => 2)
          %br/
      / <haml:loud> link_to 'Edit', edit_purchase_path(@purchase) </haml:loud> |
      = link_to 'Back', purchases_path, :class => :button
