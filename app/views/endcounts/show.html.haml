.block
  .content
    /%h2.title New purchase
    .inner
      %p
        %b Beginning date:
        = @endcount.begin_date.strftime("%F")
      %p
        %b End date:
        = @endcount.end_date.strftime("%F")
      = link_to 'Edit', edit_endcount_path(@endcount)
      |
      \#{link_to 'Back', endcounts_path}
