= simple_form_for @restaurant do |f|
  = f.input :store_id
  = f.input :name
  = f.association :company, :prompt => '(select)'
  = f.input :description
  = f.input :currency_id
  .actions
    =f.button :submit
