.block
  .content
    .inner
      %p
        %b Item name:
        = @inventoryitem.name
      %p
        %b Branch:
        = @inventoryitem.branch.location
      %p
        %b Unit:
        = @inventoryitem.unit.name
      = link_to 'Edit', edit_inventoryitem_path(@inventoryitem)
      |
      \#{link_to 'Back', inventoryitems_path}
