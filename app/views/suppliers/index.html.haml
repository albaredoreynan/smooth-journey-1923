.block
  = render 'shared/menu/database'
  .content
    .title_action_bar
      %h2.title Suppliers
      .new_actions
        = link_to 'New Supplier', new_supplier_path, :class => 'button'
    .inner
      %table
        %tr
          %th Company
          %th Name
          %th Email
          %th Address
          %th Description
          %th Contact Number
          %th.actions Actions
        - @suppliers.each do |supplier|
          %tr
            %td= supplier.company.name unless supplier.company.nil?
            %td= supplier.name
            %td= supplier.email
            %td= supplier.address
            %td= supplier.description
            %td= supplier.contact_number
            %td
              = link_to 'Show', supplier
              = link_to 'Edit', edit_supplier_path(supplier)
              = link_to 'Destroy', supplier, :confirm => 'Are you sure?', :method => :delete
