%nav#main-navigation
  = tabs_tag :builder => MenuTabBuilder do |tab|
    -#= tab.sales 'Sales', sales_path
    = tab.purchases 'Purchases', purchases_path
    = tab.inventory 'Inventory', endcounts_path
    -#= tab.employees 'Employees', employees_path
    = tab.reports 'Reports', endcount_reports_path
    - default_database_path = can? :read, Company ? companies_path : branches_path
    = tab.database 'Database', default_database_path
    - if can? :manage, Setting
      = tab.settings 'Settings', '/settings'
