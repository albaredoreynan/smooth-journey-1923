.block
  .content
    %h2= "Unit: #{@unit.symbol}"
    .inner
      %p= @unit.name
      - if can? :edit, Unit
        = link_to 'Edit', edit_unit_path(@unit)
        |
      \#{link_to 'Back', units_path}
