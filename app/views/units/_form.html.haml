= form_for(@unit) do |f|
  - if @unit.errors.any?
    #error_explanation
      %h2
        = pluralize(@unit.errors.count, "error")
        prohibited this unit from being saved:
      %ul
        - @unit.errors.full_messages.each do |msg|
          %li= msg
  %table
    %tr
  .field
  %td= f.label :unit_name
  %td= f.text_field :unit_name
  .actions
  %td= f.submit
